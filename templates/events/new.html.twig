{% extends 'base.html.twig' %}

{% block title %}New Events{% endblock %}

{% block body %}
<div class="container">
    <h1>Create new Events</h1>

    {{ include('events/_form.html.twig') }}

    <a class="btn btn-primary mt-2" href="{{ path('app_events_index') }}">back to list</a>
    </div>
{% endblock %}
